const Renderer=(()=>{let canvas,ctx;const SCALE=40;let offsetX=300,offsetY=300;function init(c){canvas=c;ctx=canvas.getContext('2d')}function worldToScreen(wx,wy){return[offsetX+wx*SCALE,offsetY-wy*SCALE]}function clear(){ctx.fillStyle='#111';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle='#1e1e1e';ctx.lineWidth=1;for(let x=0;x<canvas.width;x+=SCALE){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,canvas.height);ctx.stroke()}for(let y=0;y<canvas.height;y+=SCALE){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(canvas.width,y);ctx.stroke()}const[ox,oy]=worldToScreen(0,0);ctx.strokeStyle='#333';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(ox-8,oy);ctx.lineTo(ox+8,oy);ctx.stroke();ctx.beginPath();ctx.moveTo(ox,oy-8);ctx.lineTo(ox,oy+8);ctx.stroke()}function drawWalls(walls){for(const wall of walls){const segs=Physics.getWallSegments(wall);const mat=Physics.MATERIALS[wall.material]||Physics.MATERIALS.concrete;ctx.strokeStyle=mat.color;ctx.lineWidth=wall.material==='glass'||wall.material==='acrylic'?2:3;if(wall.material==='glass'||wall.material==='acrylic'){ctx.setLineDash([6,4])}else{ctx.setLineDash([])}for(const s of segs){const[sx1,sy1]=worldToScreen(s.x1,s.y1);const[sx2,sy2]=worldToScreen(s.x2,s.y2);ctx.beginPath();ctx.moveTo(sx1,sy1);ctx.lineTo(sx2,sy2);ctx.stroke()}}ctx.setLineDash([])}function drawVehicle(vehicle){const[sx,sy]=worldToScreen(vehicle.x,vehicle.y);const aRad=Physics.deg2rad(vehicle.angle);const startRad=Physics.deg2rad(vehicle.angle+Physics.LIDAR_START_DEG-90);const endRad=Physics.deg2rad(vehicle.angle+Physics.LIDAR_END_DEG-90);ctx.fillStyle='rgba(100,100,100,0.08)';ctx.beginPath();ctx.moveTo(sx,sy);ctx.arc(sx,sy,Physics.LIDAR_MAX_RANGE*SCALE,-endRad,-startRad,false);ctx.closePath();ctx.fill();const size=8;ctx.fillStyle='#ccc';ctx.beginPath();ctx.moveTo(sx+Math.cos(-aRad)*size*1.5,sy+Math.sin(-aRad)*size*1.5);ctx.lineTo(sx+Math.cos(-aRad+2.4)*size,sy+Math.sin(-aRad+2.4)*size);ctx.lineTo(sx+Math.cos(-aRad-2.4)*size,sy+Math.sin(-aRad-2.4)*size);ctx.closePath();ctx.fill()}function drawRays(vehicle,scanPoints){const[sx,sy]=worldToScreen(vehicle.x,vehicle.y);for(const p of scanPoints){const[hx,hy]=worldToScreen(p.hitX,p.hitY);const alpha=p.material?0.3:0.05;ctx.strokeStyle=`rgba(150,150,150,${alpha})`;ctx.lineWidth=0.5;ctx.beginPath();ctx.moveTo(sx,sy);ctx.lineTo(hx,hy);ctx.stroke();if(p.material){const bright=Math.floor(55+(p.intensity/255)*200);ctx.fillStyle=`rgb(${bright},${Math.floor(bright*0.7)},${Math.floor(bright*0.4)})`;ctx.beginPath();ctx.arc(hx,hy,2.5,0,Math.PI*2);ctx.fill()}}}function setPan(dx,dy){offsetX+=dx;offsetY+=dy}return{init,clear,drawWalls,drawVehicle,drawRays,setPan,SCALE}})();
